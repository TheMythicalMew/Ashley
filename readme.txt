Put your code into Index

Change package insides to what you need

change config.json.example to config.json and input your token

to load diffrent code files  do const name = require('./otherfile.js')